query Input {
  presentmentCurrencyRate
  cart {
    lines {
      id
      shopCurrencyPrice: attribute(key: "_custom_price_shop") {
        value
      }
      legacyPrice: attribute(key: "_custom_price") {
        value
      }
    }
  }
}
