{%- doc -%}
  Renders the cart summary totals.
{%- enddoc -%}

{%- liquid
  assign adjusted_total_price = cart.total_price
  for item in cart.items
    assign sale_cp = item.properties['_Sale_Price'] | times: 1
    if sale_cp > 0
      assign property_line_price = sale_cp | times: item.quantity
      if item.final_line_price > property_line_price
        assign line_price_difference = item.final_line_price | minus: property_line_price
        assign adjusted_total_price = adjusted_total_price | minus: line_price_difference
      endif
    endif
  endfor
  if adjusted_total_price < 0
    assign adjusted_total_price = 0
  endif
  assign total_price = adjusted_total_price | money
-%}

<div class="cart__ctas">
  <button
    type="submit"
    id="checkout"
    class="cart__checkout-button button"
    name="checkout"
    {% if cart == empty %}
      disabled
    {% endif %}
    form="cart-form"
  >
    {{ 'content.checkout' | t }} —
    <text-component
      ref="cartTotal"
      value="{{ total_price | strip_html }}"
      data-cart-subtotal
    >{{ total_price }}</text-component>
  </button>

  {% if additional_checkout_buttons and settings.show_accelerated_checkout_buttons %}
    <div class="additional-checkout-buttons additional-checkout-buttons--vertical">
      {{ content_for_additional_checkout_buttons }}
    </div>
  {% endif %}
</div>

{% stylesheet %}{% endstylesheet %}
